1. Выполнение операций на UI-потоке (тяжелые вычисления, загрузка данных/изображений).

2. Работа с изображениями:
    2.1. избыточное разрешение;
    2.2. отсутствие placeholder;
    2.3. отсутствие кэширования.

3. Отсутствие ленивой инициализции объектов.

4. Утечки памяти.

5. Избыточные рекомпозиции:
    5.1. использование нестабильных типов;
    5.2. передача State по всей иерархии Composable-дерева;
    5.3. лямбды.

6. Списки:
    6.1. отсутствие key;
    6.2. отсутствие contentType;
    6.3. Column/Row.scrollable() для "больших" списков;
    6.4. LazyRow/LazyColumn для "маленьких" списков;
    6.5. Отображение элементов размером 0.px, если хотим скрыть контент (компилятору придется
            заново пересчитывать размеры всех элементов на экране и их расположение)
    6.6. Создание объекта для каждого элемента списка.

// сначала вычислить X
7. Глубокая вложенность Composable-функций;

// сначала проверить предположение
8. Использование Android View внутри Composable-функции.
